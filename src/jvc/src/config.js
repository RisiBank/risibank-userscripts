// Base RisiBank origin used for every API call, media/cache URL and the embed.
// To build against a local/dev instance, change this to e.g.:
//   export const RISIBANK_URL = 'https://risibank.fr';
// Use a bare origin (scheme + host + port), no trailing slash.
export const RISIBANK_URL = 'https://risibank.fr';
